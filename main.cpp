#include <bits/stdc++.h>
using namespace std;

int main()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    int tc;
    cin >> tc;

    while (tc--)
    {
        cout << "Hello world!" << endl;
    }

    return 0;
}